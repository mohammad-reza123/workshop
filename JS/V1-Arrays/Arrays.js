let todoList = ["ithem1","ithem2","ithem3"];

//call the array ithem with the index 
//indexes starts with zero(0)
todoList[1]; //output is "ithem2"

//add an Array ithem ( .push)
todoList.push("ithem4");
todoList.push("hoy kooni im here");

//delete an ithem in Array 
todoList.splice(1,1);//the forst one is for the ithem and seconde one is for how many u want delete
todoList.splice(0,2);//start to index 0 and delete 2 elements.

//change Array ithem
todoList[1] = "somethingelse";//define an index and asigne a new value
